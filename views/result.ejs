<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>What If World - Scenario Result</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <link rel="stylesheet" href="css/chart.css">
</head>
<body>
  <div class="container">
    <h2>Scenario: <%= result.textual_analysis.scenario %></h2>
    <p><strong>Domain:</strong> <%= result.textual_analysis.domain %></p>
    <p><strong>Probability:</strong> <mark><%= result.textual_analysis.probability %>%</mark> </p>

    <div class="section">
      <h3>Key Implications:</h3>
      <ul>
        <% result.textual_analysis.key_implications.forEach(item => { %>
          <li><%= item %></li>
        <% }) %>
      </ul>
    </div>

    <div class="section">
      <h3>Potential Challenges:</h3>
      <ul>
        <% result.textual_analysis.potential_challenges.forEach(item => { %>
          <li><%= item %></li>
        <% }) %>
      </ul>
    </div>

    <div class="section">
      <h3 class="chart-title">Statistical Impact Overview with bar chart</h3>
      <canvas id="scenarioChart"></canvas>
    </div>

    <hr>

    <div class="section">
      <h3 class="chart-title">Statistical Impact Overview with Pie chart</h3>
      <canvas id="scenarioChart-pie" width="500" height="500"></canvas>
    </div>
  </div>

  <hr>

  <div>
    <p>Do you want allow this to post in community</p>
    <form action="/submit-result" method="POST">
      <input type="hidden" name="resultId" value="<%= resultId %>">
      <button type="submit" name="action" value="yes">Yes</button>
      <button type="submit" name="action" value="no">No</button>
    </form>
  </div>
  <script>
      const stats = <%- JSON.stringify(result.statistics) %>;
      // expose resultId both as a window property and a global var so external scripts can access it
      window.resultId = '<%= resultId %>';
      var resultId = '<%= resultId %>';
  </script>
  <script src="js/chart-script.js">  
</script>
</body>
</html>
