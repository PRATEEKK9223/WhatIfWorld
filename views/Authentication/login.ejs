<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body>
    <% if(error_msg.length){ %>
        <div>
            <%=error_msg%>
        </div>
    <%}%>
    <h>Login</h>
    <form action="/login" method="POST">
        <div>
            <label for="email">Email</label>
            <input type="email" placeolder="Enter name" name="email">
        </div>
        <div>
            <label for="password">Password</label>
            <input type="password" placeolder="Enter password" name="password">
        </div>
        <button type="submit">Login</button>
    </form>
    <a href="/auth/google">SignIn with Google</a>
</body>
</html>